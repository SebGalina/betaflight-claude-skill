# pip fallback — canonical source of truth is pyproject.toml
# regenerate with: uv export --format requirements-txt > requirements.txt
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# compute core — the single source of truth (decoder + chirp/step/spectral + HTML report).
betaflight-chirp-core==0.1.8

# run_evals.py
anthropic>=0.40.0
python-dotenv>=1.0.0
